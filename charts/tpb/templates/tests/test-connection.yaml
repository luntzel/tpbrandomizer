apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tpb.fullname" . }}-test-connection"
  labels:
    {{- include "tpb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tpb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
