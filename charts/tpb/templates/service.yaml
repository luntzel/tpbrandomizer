apiVersion: v1
kind: Service
metadata:
  name: {{ include "tpb.fullname" . }}
  labels:
    {{- include "tpb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tpb.selectorLabels" . | nindent 4 }}
