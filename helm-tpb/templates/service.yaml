apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-tpb.fullname" . }}
  labels:
    {{- include "helm-tpb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-tpb.selectorLabels" . | nindent 4 }}
